# API reference

The API documentation is generated with Typedoc from the TypeScript sources
and published to GitHub Pages on every push to `main`:

https://dmoove.github.io/cdk-gitlab-runner/

To build it locally run `npm run docgen` and open `docs/README.md`.
